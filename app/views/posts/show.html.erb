

<strong>Name:</strong>
<%= @user.name %>

<strong>Title:</strong>
<%= @post.title %>

<strong>Content:</strong>
<%= @post.content %>

<strong>Image:</strong>
<%= image_tag(@post.image.url) if @post.image && @post.image.url %>